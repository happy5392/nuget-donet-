<Project Sdk="Microsoft.NET.Sdk">

    <!-- Import custom settings -->
    <Import Project="CustomSettings.props" />

    <!-- Define other project properties -->
    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net8.0</TargetFramework>
    </PropertyGroup>

    <!-- Define package reference -->
    <ItemGroup>
        <PackageReference Include="MyNuGetPackage" />
        <PackageReference Include="lovelyshiva" />
    </ItemGroup>

</Project>

